name: Test Publisher
on:
  workflow_dispatch:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Publisher
        uses: AmazeeLabs/publisher@b5c37ac484e65cf5b2579fe5da1d665fa345fc53
